# ==============================================================================
# 模型配置 (Model Configuration)
# ==============================================================================

# RKLLM 模型文件的绝对路径
# Absolute path to the .rkllm model file
MODEL_PATH=/path/to/your/model.rkllm

# 目标平台 (Target Platform)
# Options: rk3588, rk3576, rv1126b, rk3562
TARGET_PLATFORM=rk3588

# 对外暴露的模型名称 (Model Name in API)
# The name clients will use in API calls (e.g., "qwen-1.7b")
MODEL_NAME=rkllm-model

# RKLLM 运行时库路径 (RKLLM Runtime Library Path)
# Path to the directory containing librkllmrt.so or the full path to the .so file
# Default: lib/librkllmrt.so
RKLLM_LIB_PATH=lib/librkllmrt.so

# LoRA 适配器路径 (可选)
# Path to LoRA adapter if used
LORA_MODEL_PATH=

# Prompt Cache 路径 (可选)
# Path to save/load prompt cache
PROMPT_CACHE_PATH=

# ==============================================================================
# 服务器配置 (Server Configuration)
# ==============================================================================

# 监听地址 (Bind Host)
HOST=0.0.0.0

# 监听端口 (Bind Port)
PORT=8080

# API 密钥 (可选)
# Optional API Key for authentication
API_KEY=

# ==============================================================================
# 推理参数默认值 (Inference Defaults)
# ==============================================================================

# 最大上下文长度 (Max Context Length)
MAX_CONTEXT_LEN=4096

# 最大生成 Token 数 (Max New Tokens)
MAX_NEW_TOKENS=4096

# Top K 采样
TOP_K=1

# Top P 采样
TOP_P=0.9

# 温度 (Temperature)
TEMPERATURE=0.8

# 重复惩罚 (Repeat Penalty)
REPEAT_PENALTY=1.1

# 频率惩罚 (Frequency Penalty)
FREQUENCY_PENALTY=0.0

# 存在惩罚 (Presence Penalty)
PRESENCE_PENALTY=0.0

# ==============================================================================
# 日志配置 (Logging Configuration)
# ==============================================================================

# 日志级别 (Log Level)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志目录 (Log Directory)
LOG_DIR=logs

# 单个日志文件最大大小 (字节) (Max Log File Size in Bytes)
# 1MB = 1048576
LOG_MAX_BYTES=1048576

# 保留的旧日志文件数量 (Log Backup Count)
LOG_BACKUP_COUNT=5
